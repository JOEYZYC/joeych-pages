---
import type { Project } from "../content/schemas"
import type { Locale } from "../i18n/locales"
import type { RelatedAchievement } from "../lib/related-achievements"
import ProjectDetail from "./ProjectDetail.astro"

type Props = {
  readonly locale: Locale
  readonly project: Project
  readonly relatedAchievements: readonly RelatedAchievement[]
}

const { locale, project, relatedAchievements } = Astro.props
---

<article
  id={project.id}
  class="project-record"
  data-project-record
  data-project-id={project.id}
  data-project-key={`project:${project.id}`}
  data-project-category={project.category.en}
  data-project-title={project.title[locale]}
  data-active="false"
>
  <ProjectDetail idPrefix={project.id} locale={locale} mode="record" project={project} relatedAchievements={relatedAchievements} />
</article>
