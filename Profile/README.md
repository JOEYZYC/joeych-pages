# Profile Placeholder

`Profile/` is reserved for the shared, portable public profile package used by both site contracts.

Public data and media will be added in a later relocation task. Local private material belongs only under ignored `Profile/private/` and must never be staged or published.
